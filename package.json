{
  "name": "beppp-app",
  "version": "1.0.0",
  "dependencies": {
    "prisma": "6.9.0",
     "@prisma/client": "6.9.0"
  },
  "scripts": {
    "migrate": "prisma migrate deploy",
    "generate": "prisma generate",
    "build": "python3 -m prisma generate"
  },
  "engines": {
  "node": "22.x"
}
}
